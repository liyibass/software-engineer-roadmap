# [rust-0-2] Rust 站在哪：和 C/C++、Go、TypeScript/C# 的定位比較

> **本章目標**：把 Rust 放進「程式語言地圖」裡，看清楚它和你可能聽過的其他語言各自擅長什麼、犧牲什麼，幫你判斷「什麼時候該選 Rust」。

## 你會學到

- 用兩個軸（執行速度、開發便利）把常見語言排排站
- Rust 和 C/C++、Go、TypeScript/C# 各自的取捨
- 「系統程式語言」vs「應用程式語言」是什麼意思
- 什麼情境適合選 Rust、什麼情境其實不必

## 概念說明

### 沒有「最好」的語言，只有「最適合某情境」的語言

每種語言都是一組**取捨**的結果。與其問「哪個語言最強」，不如問「這個工作，我願意拿什麼換什麼」。我們用兩個常見的軸來看：

- **執行速度 / 對底層的掌控**：程式跑得多快、能不能精細控制記憶體。
- **開發便利 / 上手速度**：寫起來多快、多省心、多容易學。

```mermaid
quadrantChart
    title 語言定位（越右越快/越底層，越上越好寫/越省心）
    x-axis "慢、抽象高" --> "快、貼近底層"
    y-axis "難寫、要顧很多" --> "好寫、省心"
    quadrant-1 "又快又好寫(理想)"
    quadrant-2 "好寫但較慢"
    quadrant-3 "難寫又慢(少見)"
    quadrant-4 "快但要很費心"
    "C / C++": [0.95, 0.2]
    "Rust": [0.9, 0.5]
    "Go": [0.7, 0.75]
    "C#": [0.55, 0.8]
    "TypeScript": [0.4, 0.85]
    "Python": [0.3, 0.9]
```

這張圖在表達一個粗略印象：**越往右，程式越快、越能掌控底層；越往上，越好寫、越省心。** Rust 待在「很右邊」（快、貼近底層，和 C/C++ 同級），但比 C/C++「往上挪了一些」——因為它的安全保證讓你少踩很多坑。注意這是示意圖，實際取捨依場景而定，別當成精確排名。

### 一個一個比

**Rust vs C / C++**

同樣是「系統程式語言」（能直接掌控記憶體、沒有 GC），效能同級。差別在**安全**：C/C++ 把「不要踩記憶體地雷」的責任完全交給你；Rust 用編譯器幫你檢查，擋掉一整類記憶體與並行 bug（[rust-0-1] 講過）。代價是 Rust 編譯器很嚴格，要先學會所有權。

> **一句話**：想要 C/C++ 的速度，但不想再為記憶體 bug 熬夜除錯 → Rust。

**Rust vs Go**

Go 也是為「現代後端、雲端服務」而生，而且**非常好上手**、內建好用的並行模型。但 Go **有 GC**。所以：

- 要「快速開發一個普通後端服務、團隊好招人」→ Go 往往更省事。
- 要「極致效能、可預測的延遲（不能有 GC 暫停）、或要寫系統/嵌入式」→ Rust 更合適。

**Rust vs TypeScript / C#（你在 basic、csharp 課學的）**

TypeScript、C# 是**應用程式語言**——有 GC、生態成熟、開發飛快，做網站、後端 API、企業系統都很順。它們和 Rust 不太算「競爭對手」，而是**不同層次的工具**：

| | TypeScript / C# | Rust |
|---|---|---|
| 記憶體 | GC 自動管 | 所有權，編譯期管 |
| 上手 | 快 | 慢（所有權要學） |
| 典型用途 | 網站、後端 API、企業應用 | 系統、高效能服務、嵌入式、WASM |
| 心智負擔 | 低 | 高（但換來安全與速度） |

### 「系統程式語言」到底是什麼意思

你會一直聽到「Rust 是系統程式語言」。簡單說：

- **應用程式語言**：寫「給人用的軟體」（網站、App、後端服務），通常站在 GC 與作業系統的肩膀上，不太碰底層。
- **系統程式語言**：寫「給機器/其他軟體用的底層東西」（作業系統、瀏覽器引擎、資料庫、驅動程式、嵌入式韌體），需要直接掌控記憶體與硬體、不能有額外開銷。

Rust 的野心是：**讓你能寫系統級的東西，卻不必承受 C/C++ 那種隨時出包的風險。**

## 程式碼範例

這章是概念比較，沒有要你寫程式。但給你一個「同一件事、不同語言味道」的對照感受——印出一行字：

```rust
// Rust
fn main() {
    println!("Hello");
}
```

```typescript
// TypeScript
console.log("Hello");
```

```go
// Go
package main
import "fmt"
func main() { fmt.Println("Hello") }
```

看起來都差不多對吧？**語言的差異不在「印一行字」這種小事**，而在「當程式變大、要管理大量資料與並行時，誰幫你擋住錯誤、誰跑得更快」。那才是選 Rust 或不選 Rust 的真正分水嶺。

## 小練習

1. 用「取捨」的角度，各寫一句話總結：為什麼有人選 Go 而不選 Rust？為什麼有人選 Rust 而不選 C++？
2. 假設以下三個專案，你會傾向選哪個語言、為什麼？（沒有標準答案，重點是說出理由）
   - (a) 一個公司內部用的請假系統網站
   - (b) 一個要塞進智慧手錶、電力與記憶體都很拮据的韌體
   - (c) 一個每秒要處理數十萬筆、延遲不能有突然尖刺的即時交易引擎
3. 查一個你有興趣的知名軟體（例如某瀏覽器、某資料庫），看它底層用什麼語言寫，想想為什麼。

<details>
<summary>參考解答</summary>

**第 1 題（用取捨角度各寫一句話）**

- **為什麼有人選 Go 而不選 Rust？** 因為 Go 願意「用一點 GC 的開銷」去換「非常好上手、開發飛快、團隊好招人、並行模型內建又簡單」——當專案是普通後端服務、效能不到極致要求時，這筆交易很划算。
- **為什麼有人選 Rust 而不選 C++？** 因為 Rust 給你和 C++ 同級的速度與底層掌控，卻用編譯器幫你擋掉一整類記憶體與並行 bug——你願意先付「學所有權」的學費，換來「不必再為記憶體 bug 熬夜除錯」的安心。

**第 2 題（三個專案的語言選擇，重點在理由）**

沒有標準答案，示範一種合理的判斷：

- **(a) 公司內部請假系統網站** → 傾向 **TypeScript / C#（或 Go）**。這是典型應用程式：效能需求普通、重點是開發快、生態成熟、好維護、好招人。用 Rust 是殺雞用牛刀，反而拖慢開發。
- **(b) 塞進智慧手錶、電力與記憶體都拮据的韌體** → 傾向 **Rust（或 C/C++）**。這是嵌入式系統：沒有多餘資源養 GC、要直接掌控記憶體與硬體。Rust 能給你這種底層掌控，又比 C/C++ 安全，很合適。
- **(c) 每秒數十萬筆、延遲不能有尖刺的即時交易引擎** → 傾向 **Rust（或 C++）**。關鍵在「可預測的低延遲」：有 GC 的語言可能因為 GC pause 突然卡一下，對交易引擎是致命傷。Rust 沒有 GC、效能又頂級，正中要害。

判斷主軸：**先問「效能/延遲/資源是不是核心約束」——是，就往 Rust/C++ 靠；不是，就選開發更快、生態更成熟的應用語言。**

**第 3 題（查知名軟體的底層語言）**

這題要你自己查，示範幾個常見答案與原因：

- **Chrome / V8 引擎**：C++。要極致效能與對記憶體的精細掌控（瀏覽器引擎是典型系統級軟體）。
- **PostgreSQL**：C。資料庫要直接管理記憶體、磁碟、快取，效能敏感，且歷史悠久。
- **Discord 後端**：早期用 Go，後來把高負載服務改寫成 **Rust**——正是為了消除 GC pause 帶來的延遲尖刺（呼應第 2 題 (c) 的情境）。

共通觀察：**越貼近底層、越要求效能與延遲穩定的軟體，越傾向用系統程式語言（C/C++/Rust）。**

</details>

## 課外讀物

> 想理解「應用層 vs 系統層、軟體的層次」 → **cs 課程（計算機概論）Part 8：軟體與抽象**

> 想看「有 GC 的語言」怎麼把後端寫得又快又舒服 → **csharp 課程** 與 **basic 課程 Part 4（前後端串接）**
