# [infra-6-2] Cron 與排程：讓維護任務自動定時執行

> **本章目標**：學會用 cron 設定「定時自動執行」的任務，例如每天備份、每小時清理，把 Part 6-1 寫的腳本變成「不用你記得、時間到自己跑」。

## 你會學到

- cron 是什麼——伺服器的「鬧鐘 / 行事曆」
- 看懂 crontab 的五欄位時間格式
- 設定定時任務（搭配你的腳本）
- cron 與 systemd timer 的關係

## 概念說明

### 痛點：有些事得「定時做」，但人會忘記

很多維運工作不是做一次就好，而是要**規律地重複**：

- 每天凌晨備份資料庫
- 每小時清理暫存檔
- 每週產生一份報表
- 每分鐘檢查服務還活著嗎

這些事如果靠人「記得去做」，遲早會忘、會漏。Part 4-4 我們也看過——certbot 的憑證續期就是「定時自動」做的，沒人會記得每 90 天手動續一次。

**cron** 就是解決這件事的工具：你告訴它「**什麼時間、執行什麼**」，它就會像鬧鐘一樣，時間到自動幫你跑，不用你盯著。

用類比：cron 是伺服器的**行事曆鬧鐘**——你把「每天早上 3 點：備份」寫進去，它每天 3 點準時響、準時做，風雨無阻。

---

### cron 的時間格式：五個欄位

cron 的設定（叫 **crontab**，cron table）每一行長這樣：**五個時間欄位 + 要執行的指令**。

```
┌── 分鐘 (0-59)
│ ┌── 小時 (0-23)
│ │ ┌── 日 (1-31)
│ │ │ ┌── 月 (1-12)
│ │ │ │ ┌── 星期 (0-7，0 和 7 都是星期日)
│ │ │ │ │
* * * * *  要執行的指令
```

每個欄位填數字代表「在這個時間點」，填 `*` 代表「每一個都算」（不限制）。看幾個例子最快懂：

| 設定 | 意思 |
|------|------|
| `0 3 * * *` | **每天**凌晨 3:00 |
| `*/10 * * * *` | **每 10 分鐘**（`*/10` = 每隔 10） |
| `0 * * * *` | **每小時**整點（第 0 分） |
| `0 0 * * 0` | **每週日**午夜 |
| `30 2 1 * *` | **每月 1 號**的 2:30 |

口訣：**從左到右是「分、時、日、月、週」**。一開始會不習慣，多看幾個例子、或用線上的 cron 產生器輔助就熟了。

---

### 視覺化：cron 怎麼運作

```mermaid
graph LR
    CRON["cron 服務<br/>（背景常駐的鬧鐘）"]
    TAB["crontab<br/>（你寫的時間表）"]
    JOB1["每天 3:00 → 備份腳本"]
    JOB2["每 10 分 → 健康檢查"]

    TAB --> CRON
    CRON -->|"時間到，自動執行"| JOB1
    CRON -->|"時間到，自動執行"| JOB2
```

這張圖在說：cron 是個一直待命的服務，它讀你的時間表，時間一到就自動觸發對應的任務。

## 程式碼範例

### 編輯你的 crontab

每個使用者有自己的 crontab。編輯它：

```bash
crontab -e
```

第一次會問你選編輯器，選 `vi`（或 `vim`）。打開後，在最下面加一行。例如「每天凌晨 3 點，執行 Part 6-1 的健康檢查腳本」：

```
0 3 * * * /home/deploy/infra-practice/healthcheck.sh
```

> 重要：cron 裡**一定要用完整路徑**（`/home/deploy/...`，不能用 `~` 或相對路徑），因為 cron 執行時的環境跟你登入時不一樣，它不知道你的「家」在哪。

存檔離開，這條排程就生效了。

---

### 把輸出存進日誌

腳本跑的時候會有輸出，但 cron 在背景跑，你看不到。最好把輸出「導向」到一個日誌檔，之後出問題能查：

```
0 3 * * * /home/deploy/infra-practice/healthcheck.sh >> /home/deploy/logs/health.log 2>&1
```

拆解結尾那串：

- `>> /home/deploy/logs/health.log`：把輸出**附加**到這個日誌檔（`>>` 是附加，`>` 是覆蓋）。
- `2>&1`：把「錯誤訊息」也一起導到同一個檔（`2` 是錯誤輸出、`1` 是標準輸出，這串意思是「錯誤也跟著正常輸出走」）。

這樣每次執行的結果都會留下記錄，呼應 Part 1-1 說的「能查線索」的維運習慣。

---

### 查看與管理排程

列出你目前所有的排程：

```bash
crontab -l
```

`-l` 是 list。要修改就再 `crontab -e`，把那行刪掉或改掉即可。

---

### cron 與 systemd timer

你在 Part 4-4 看過 `certbot.timer`——那是 **systemd timer**，是另一種排程方式。它和 cron 做的事類似，但和 systemd 整合更好（能用 `journalctl` 看日誌、有更靈活的觸發條件）。

簡單分辨：

| | cron | systemd timer |
|---|------|--------------|
| 設定方式 | `crontab -e`，一行搞定 | 寫 `.timer` + `.service` 兩個檔 |
| 適合 | 簡單的定時任務 | 需要和服務整合、複雜觸發 |
| 上手難度 | 低 | 中 |

日常簡單排程用 cron 就很夠了；當你需要更進階的控制，再認識 systemd timer。兩者並存，不衝突。

> WSL 注意：cron 在 WSL 上可用，但要確保 cron 服務有在跑（`sudo service cron start`，或 Part 0 啟用 systemd 後用 `systemctl`）。WSL 沒開著的時候排程不會執行——所以「每天凌晨自動跑」這類任務，正式環境還是要放在一直開機的雲端伺服器上。

## 小練習

### 練習 1：翻譯 cron 時間

不查表，說出下面各代表什麼時間：

1. `30 1 * * *`
2. `*/5 * * * *`
3. `0 0 * * 1`

<details>
<summary>參考解答</summary>

記住口訣「從左到右是分、時、日、月、週」，逐欄對照：

1. `30 1 * * *` → 分=30、時=1，日/月/週都是 `*`（每一個都算）。所以是**每天凌晨 1:30**。
2. `*/5 * * * *` → 分=`*/5`（每隔 5 分鐘），其餘都是 `*`。所以是**每 5 分鐘執行一次**。
3. `0 0 * * 1` → 分=0、時=0（午夜），週=1（星期一）。所以是**每週一的午夜 0:00**（記得週的 0 和 7 都是星期日，1 才是星期一）。

</details>

---

### 練習 2：設一個排程

幫你的 `healthcheck.sh` 設一個「每 30 分鐘執行一次、並把結果寫進日誌」的 cron。設好後用 `crontab -l` 確認。

> 提示：每 30 分鐘是 `*/30 * * * *`。

<details>
<summary>參考解答</summary>

先開啟你的 crontab：

```bash
crontab -e
```

在最下面加上這一行（記得先確認日誌資料夾存在，可先跑 `mkdir -p /home/deploy/logs`）：

```
*/30 * * * * /home/deploy/infra-practice/healthcheck.sh >> /home/deploy/logs/health.log 2>&1
```

拆解這一行：

- `*/30 * * * *`：每 30 分鐘觸發一次。
- `/home/deploy/infra-practice/healthcheck.sh`：要執行的腳本，**一定要用完整路徑**（不能用 `~`）。
- `>> /home/deploy/logs/health.log`：把輸出附加進日誌檔（`>>` 是附加，不會蓋掉舊記錄）。
- `2>&1`：把錯誤訊息也一起導進同一個日誌，之後出問題查得到。

存檔離開後，用 `crontab -l` 確認那行有出現：

```bash
crontab -l
```

驗收：等 30 分鐘（或先把腳本手動跑一次確認能執行），再看 `cat /home/deploy/logs/health.log` 有沒有留下輸出。要提醒的是，這需要 cron 服務有在跑；在 WSL 上要先 `sudo service cron start`，而且 WSL 沒開機時排程不會執行。

</details>

---

### 練習 3：理解「完整路徑」的重要

回答：為什麼 cron 裡的指令一定要寫完整路徑，不能用 `~` 或相對路徑？如果寫了相對路徑，最可能發生什麼事？

> 提示：cron 執行的環境，跟你 SSH 登入後的環境不一樣，它不知道你「現在站在哪、家在哪」。

<details>
<summary>參考解答</summary>

**為什麼要用完整路徑**：cron 執行任務時，用的是一個很「乾淨、精簡」的環境，跟你 SSH 登入後拿到的環境不一樣。差別主要有兩點：

1. **家目錄與 `~` 不一定如你所想**：`~` 是「當前使用者的家目錄」的縮寫，但 cron 執行時的家目錄設定可能跟你登入時不同，甚至沒設好，`~` 會解析錯地方。
2. **工作目錄不是你以為的地方**：相對路徑（例如 `./healthcheck.sh` 或 `infra-practice/healthcheck.sh`）是「相對於當前工作目錄」而言。但 cron 執行時的工作目錄通常是家目錄或某個預設路徑，不是你上次 `cd` 進去的那個資料夾。

**寫了相對路徑最可能發生什麼事**：cron 在它的工作目錄裡找不到那個檔案，任務直接失敗，錯誤大概是 `No such file or directory` 或 `command not found`。而且因為 cron 在背景跑、你當下看不到，這種錯常常是「排程默默沒作用，過了好幾天才發現」——這也是為什麼上一題要把輸出（含 `2>&1` 的錯誤）導進日誌，才查得到線索。

結論：cron 裡一律寫**絕對路徑**（從 `/` 開頭），包含腳本路徑和腳本裡用到的其他檔案路徑，最保險。

</details>
